// primeiro arquivo em linguagem c
// vamos importar a biblioteca base para executar o nosso progama
// a blibioteca se chama stdio(std->Standart | i -> input | o  -> output)
// para importar, usaremos o comando #include
#include <stdio.h>

// a aixo, iremos escrever os comandos que serao realmente executados
// utilizaremos a funçao main(principal) para iniciar a execuçao 
int main(){

// o comando printf exibir uma mensagem em tela de console
    printf("hello, world!\n");

// informa ao compilador (tradutor) gcc que o progama acabou
    return 0;

}
